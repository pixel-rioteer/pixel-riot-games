# Site logo asset

Upload the Pixel Riot logo image as:

```text
assets/pixel-riot-logo.png
```

The site already references this path in `style.css` and on the homepage. Once the PNG exists here, it will appear in the navigation, homepage hero, and subtle page watermark.
